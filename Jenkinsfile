pipeline {
agent any
    stages {
        stage('Build and Running') {
            steps {
                bat 'docker-compose up --build'
                // echo 'It works'
            }
        }

    }
}
